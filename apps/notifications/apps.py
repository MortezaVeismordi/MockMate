from django.apps import AppConfig


class NotificationsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.notifications'  # مسیر کامل ماژول
    verbose_name = 'مدیریت اطلاع‌رسانی‌ها'
